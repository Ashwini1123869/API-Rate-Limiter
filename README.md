# API-Rate-Limiter
Developed a distributed rate limiting middleware using Node.js and Redis that restricts excessive requests using atomic counters and TTL logic, preventing server overload and improving API reliability.
